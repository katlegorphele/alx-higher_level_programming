#!/usr/bin/node

class Rectangle {
  // Define constructor which takes 2
  // arguments
  constructor (w, h) {
    // Initialize instance attributes with args
    this.width = w;
    this.height = h;
  }
}

module.exports = Rectangle;
