#!/usr/bin/node

// Define array of lines to print
const lines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Use loop to print each line in array to console
for (let i = 0; i < lines.length; i++) {
  console.log(lines[i]);
}
