#!/usr/bin/node

// Define the add function
function add(a, b) {
    return a + b;
  }
  
  // Call the add function with two integer arguments and print the result
  console.log(add(3, 5)); // Output: 8